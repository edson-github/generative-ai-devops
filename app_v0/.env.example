# App v0 - Variáveis de Ambiente
# Copie este arquivo para .env e ajuste os valores

# === PostgreSQL ===
POSTGRES_USER=app
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=appdb

# === Database URL ===
# Para ambiente local (host.docker.internal no Docker/Podman ou localhost)
DATABASE_URL=postgresql+psycopg://app:change_me_in_production@localhost:5432/appdb

# === Backend API ===
API_HOST=127.0.0.1
API_PORT=8000

# === Observações ===
# - Use senhas fortes em produção
# - Nunca commite o arquivo .env no Git
# - Este é apenas um template para desenvolvimento
