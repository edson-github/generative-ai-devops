# App v1 - Variáveis de Ambiente
# Copie este arquivo para .env e ajuste os valores

# === PostgreSQL ===
POSTGRES_USER=app
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=appdb

# === Database URL ===
# Formato: postgresql+psycopg://user:password@host:port/database
DATABASE_URL=postgresql+psycopg://app:change_me_in_production@db:5432/appdb

# === Backend API ===
API_HOST=backend
API_PORT=8000

# === Grafana (se usado) ===
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change_me_in_production

# === OpenSearch (se usado) ===
OPENSEARCH_ENABLED=false
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_SCHEME=http
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=change_me_in_production
OPENSEARCH_INDEX=logs-app-v1
OPENSEARCH_INITIAL_ADMIN_PASSWORD=change_me_in_production

# === Observabilidade ===
# Para desenvolvimento, valores simples são aceitáveis
# Para produção, use senhas fortes e secrets do Kubernetes
